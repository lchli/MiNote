<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lch.menote.note">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:icon="@mipmap/ic_launcher"
        tools:replace="android:icon">
        <activity android:name=".ui.LocalNoteDetailActivity" />
        <activity android:name=".ui.ImageGalleryActivity" />
        <activity android:name=".ui.EditNoteActivity" />
        <activity android:name=".ui.MusicActivity" />
        <activity android:name=".ui.CloudNoteDetailActivity" />
        <activity android:name=".ui.EditNoteUi" />
        <activity android:name=".ui.LocalNoteDetailUi" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.file.provider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/boxing_file_provider" />

        </provider>
    </application>

</manifest>